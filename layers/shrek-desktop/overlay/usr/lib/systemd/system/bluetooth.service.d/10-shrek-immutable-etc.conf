[Service]
# Debian's unit manages /etc/bluetooth with ConfigurationDirectory=bluetooth.
# Shrek's /etc is sealed read-only, and the config directory is prebuilt into
# the image, so do not let systemd create/chmod it at runtime.
ConfigurationDirectory=
