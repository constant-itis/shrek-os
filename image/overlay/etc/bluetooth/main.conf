[General]

# Keep Debian BlueZ defaults. This file intentionally exists so the packaged
# bluetooth.service ConfigurationDirectory=bluetooth has a sealed /etc/bluetooth
# directory and does not try to create it at runtime on the read-only root.
